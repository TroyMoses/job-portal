import React from "react";

const ApplyButton = () => {
  return (
    <div>
      <button className="px-10 rounded-lg py-3 bg-blue-600 text-white font-semibold transition-all duration-500 hover:bg-blue-900">
        Apply Now
      </button>
    </div>
  );
};

export default ApplyButton;
